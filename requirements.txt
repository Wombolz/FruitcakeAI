# FruitcakeAI v5 — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
websockets==13.1

# ── LLM / Agent ──────────────────────────────────────────────────────────────
litellm>=1.82.0

# ── RAG ──────────────────────────────────────────────────────────────────────
# Keep llama-index-core explicit to avoid pulling llama-index-llms-openai
# (which requires openai<2 and conflicts with litellm/openai>=2).
llama-index-core==0.13.6
llama-index-vector-stores-postgres==0.7.3
llama-index-embeddings-huggingface==0.6.1
llama-index-retrievers-bm25==0.6.5
# NOTE: llama-index-llms-openai is intentionally NOT listed here.
# It requires openai<2, which conflicts with litellm/openai>=2.
# We set Settings.llm = None so LlamaIndex's LLM layer is never invoked.

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg>=0.29.0,<0.30.0
psycopg2-binary>=2.9.6,<3
pgvector==0.3.6
alembic==1.14.0

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt>=3.1,<4.1   # passlib 1.7.4 incompatible with bcrypt 4.1+

# ── Config ────────────────────────────────────────────────────────────────────
pydantic-settings==2.7.0
python-dotenv==1.0.1
PyYAML==6.0.2
email-validator>=2.0

# ── HTTP / MCP ────────────────────────────────────────────────────────────────
httpx[http2]>=0.23.0,<0.28.0
# Phase 2 (MCP) — requires Python 3.10+. Uncomment when implementing Sprint 2.1:
# mcp>=1.0.0
# sse-starlette>=1.6.5

# ── Web research + RSS (Sprint 2.2) ──────────────────────────────────────────
beautifulsoup4>=4.12.0
feedparser>=6.0.0

# ── Calendar integrations (optional — install only if using) ──────────────────
# Google Calendar: pip install google-api-python-client google-auth
#   then set GOOGLE_CALENDAR_ENABLED=true in .env
caldav>=1.3.0
icalendar>=5.0.0

# ── Document parsing ─────────────────────────────────────────────────────────
pymupdf==1.25.1
python-docx==1.1.2

# ── Task scheduling (Phase 4) ─────────────────────────────────────────────────
apscheduler>=3.10,<4

# ── Observability ─────────────────────────────────────────────────────────────
structlog==24.4.0

# ── ML / Embeddings ──────────────────────────────────────────────────────────
sentence-transformers==3.3.1
torch==2.6.0
transformers==4.48.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
aiosqlite>=0.19.0      # in-memory SQLite for tests (no postgres needed)
